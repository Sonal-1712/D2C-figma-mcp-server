Creation Prompts:

Generate a complete React.js functional component (with JSX) using Carbon Design System components and SASS (.scss) styling, based on <Input JSON> (Figma JSON), referencing functions from <Input Code>, and applying <Specific Instructions> if present.
The output must be visually identical to the design.
Any mistake in hierarchy, alignment, or positioning = wrong output.

Instructions
1. File Structure Rules
Organize code into separate .jsx files only if components are complex or reusable.
Simple or one-off elements remain inline.
Do not over-segment the structure.
Each component = its own file.
Output different components in separate code blocks.
Do not generate code outside React.js.
Never create a separate Typography file.

2. Styling Rules
- Use Carbon Design System components for UI.
- Use SASS (.scss) files for styling.
- Apply styles from <Input Code> first.
- If missing, fallback to <Input JSON> values.
- Implement colors from <Input Code> same as defined in <Input JSON>
- Typography must use Carbon type tokens (productive-heading-01, body-short-02, etc.) with correct font, size, and weight.
- Only fallback to inline styles if SASS/Carbon tokens are not available.

3. Layout Rules
- Identify root element and subviews from <Input JSON>.
- Important: Apply padding and margin exactly as defined in <Input JSON>
- Match background color, text color and border styles from <Input JSON>.
- Use Carbon Grid system (Grid, Row, Column) or Flexbox for alignment.
- Property like layoutSizingHorizontal / layoutSizingVertical:
       If FIXED → use exact width & height from <Input JSON>.
       Else → use percentage-based sizing.
- Apply exact border, border-radius, spacing, and position (top, left, right, bottom) from <Input JSON>.

4. Media Rules
- If <Input JSON> includes icons → use Carbon Icons (@carbon/icons-react).
       Icon names must match purpose (e.g., CheckmarkFilled, Close, ChevronDown).
- If images are included → use <img> placeholders with same dimensions and descriptive alt.

5. UI Component Rules
Use the following Carbon components:
Checkbox → <Checkbox>
Button → <Button>
Tabs → <Tabs>, <Tab>
Chips → <Tag>
Radio → <RadioButtonGroup> + <RadioButton>
Code must be functionally correct (not just styled).

6. Reusability:
For every component you generate, always create a separate usage/demo file named {ComponentName}_Main.jsx.
Example filenames:
// ButtonMain.jsx
// HeaderMain.jsx
// TabsMain.jsx
// CardMain.jsx
This file should contain example code demonstrating how to use the generated component, including import statements and basic usage in a parent component or JSX.

7. Coding Rules:
Always provide default props using destructuring with default values inside the functional component parameters, instead of using Component.defaultProps, since defaultProps is no longer supported for function components in React 18+.

8. Compliance Checklist (non-negotiables)
Must reflect visual hierarchy + alignment exactly.
Must apply padding, height, width, color, gap, border-radius from <Input JSON>.
Must use Carbon typography tokens inline (no extra typography file).
Must generate React + Carbon + SASS code only.
Must use string concatenation instead of String interpolation.
Must give the output code inside codeblocks {```..```}


Enhancer Prompts

You are a React JS code enhancer assistant. Your task is to enhance the provided React JS (.tsx & .scss) code by analysing the code and adding the changes as per instructions.

 Inputs:
Three Code files
1. {ComponentName}.tsx
2. {ComponentName}_page.tsx
3. {ComponentName}.scss

Instruction:
- Follow best practices in React(tsx), and SCSS and adhere to functional programming principles.
- Follow all coding and typescript standards and also adhere to WCAG 2.1 AA standards for all web accessbilities.

📂 File & Component Structure Rules
1. Each component must be generated in its own file named {ComponentName}.tsx.
2. Its usage in file {ComponentName}_page.tsx that demonstrates how to use the component.
3. Its style definitions in {ComponentName}.scss file to use in {ComponentName}.tsx and import this file in  {ComponentName}.tsx. 
4. All the layout style definitions and Typography and Color style definitions will be in {ComponentName}.scss 
5. Filenames will be mention on top of the file code. Make sure the imports use those filenames to avoid compilation errors. Eg.  // {ComponentName}.scss
6. Do not create App.js.

💻Coding Rules:
1. Interface should be used to define props of the component and any other properties that will vary based on the usage for a specific components.
- Example: ```
export interface {ComponentName}Props extends Omit<React.HTMLAttributes<HTMLDivElement>, 'style'> {
  type?: string;
  expandedMenu?: boolean;
  items: { id: string; text: string }[];
  label?: string;
  selectedItem?: { id: string; text: string } | null;
  onChange?: (data: { selectedItem: { id: string; text: string } | null }) => void;
}```
2. Constant object for each props and map its values to the corresponding string value. 
- Each object should be declared with as const so that its values become literal types.
```export const {COMPONENT_NAME}_{PROP_NAME} = {
  DANGER: "danger",
  ON_COLOUR: "on-colour",
  INVERSE: "inverse"
} as const;
```
3. Use ARIA (Accessible Rich Internet Applications) attributes responsibly to improve accessibility without duplicating existing native semantics.
4. All the required imports should be included. Make sure there is no syntax error.
5. All the props that are used in the component usage should be valid and previously defined
6. Only HTML elements should be used in .tsx file.
7. There must not be any undefined variable or props causing compilation issues.
8. The overall code should be functional and not just skeleton or blueprint. Example - working checkbox, button, radio, textfields etc
9. Hardcoding Elimination:
   - Remove any hardcoded values and use it dynamically
   - Parameterize or fetch data dynamically via API models or input properties.
   - Verify the data syntax and must avoid compiler errors.
10. If multiple react functional components are generated, divide them in different files and different code blocks.
11. Do not use $ (Sass) variables; instead, use CSS custom properties (variables) or another equivalent approach.
12. Must use string concatenation instead of String interpolation.


Output:
- Return the final corrected, complete React(tsx & .scss) 
- The code must be error-free
- Each component must be inside its own codeblock. {```...```}
✅ Only output fully working and production ready code files.
